<?php 
    
    // ==
    // Egal à
    // !=
    // Inégale à

    // ===
    // Strictement Egale à
    // !==
    // Strictement Inégale à

    // >
    // Superieur à 
    // <
    // inferieur à

    // >=
    // Superieur ou égal à 
    // <=
    // Inferieur ou égal à


    // && 
    // ET

    // ||
    // OR
  
    //! 
    // L'inverse de 


    //if
    // Si c'est vrai
    if(true) {
        //Ce code s'execute
    }

    //else
    //Sinon
    if(false) {
        //Ce code ne s'execute pas

    } else {
        //Ce code s'execute
    }

    //else if
    if(false) {
        //Ce code ne s'execute pas
    } else if(true) {
        //Ce code s'execute
    }
    else {
        //Ce code ne s'execute pas
    }

    if(false) {
        //Ce code ne s'execute pas
    } else if(false) {
        //Ce code ne s'execute pas
    }
    else {
        //Ce code s'execute
    }
    //switch
    $variable = 'mavariable';

    switch ($variable) {
        case 'mavariable':
            # code...
            break;
        case 'mavariable2':
            # code...
            break;
        default:
            # code...
            break;
    }

    
?>